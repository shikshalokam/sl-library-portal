const paginationOptions = [5, 10, 25, 100]; // for pagination selection options
const initialOption = 10; // option to show initially

export const constants = {
    paginationOptions: paginationOptions,
    initialOption: initialOption
};